name: 🔌 Test FTP Raw Connection

on:
  workflow_dispatch: # Se ejecuta manualmente desde GitHub Actions

jobs:
  ftp-test:
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Test FTP connection (raw, no secrets)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.parteluzarquitectura.com   # 🔹 Cambia por tu servidor FTP real
          username: rsalgado     # 🔹 Cambia por tu usuario FTP
          password: Rs82821301+    # 🔹 Cambia por tu contraseña FTP
          local-dir: './'           # Carpeta local (cualquiera, para probar)
          server-dir: '/public_html/'  # Carpeta de prueba en tu hosting
          log-level: verbose        # 🔹 Activa log detallado
          dry-run: true             # 🔹 No sube nada, solo prueba conexión
